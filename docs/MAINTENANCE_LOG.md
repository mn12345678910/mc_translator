这个文件用于记录 `mc_translator_rs` 的历次臭蟲修復、架構調整與效能優化。

## [2026-03-11] Revision 15.20 - UI 佈局防遮擋與顏色常量化重構
- **佈局重構 (Overlap Fix)**：將導航按鈕 (`⚙`, `🌓`, `📖`, `🔧`) 從 `horizontal_wrapped` 提取並固定置右。輸出路徑標籤獨立分行並具備 `truncate` 保護，徹底解決視窗縮小時路徑文字遮擋功能按鈕的問題。
- **顏色管理常量化**：在 `ui.rs` 頂部定義 `LABEL_COLOR_LIGHT` 與 `LABEL_COLOR_DARK` 常量，取代所有散落在各渲染函數內的手動顏色定義。
- **色系深度優化**：淺色模式標籤全面更換為 **深靛藍色 (#1E3C78)**，提供更高的對比度與現代感，解決與背景色系過於接近的問題。
- **全局標籤統一**：主畫面與建議詞管理器所有標籤統一套用粗體與變數管理顏色。

## [2026-03-10] Revision 15.17 - 主視窗幾何持久化
- **跨 Session 記憶**：位置 (`main_x/y`) 與尺寸 (`main_width/height`) 現在同步至 `AppConfig`。
- **自動恢復**：啟動時自動套用上次關閉時的佈局，不再固定於預設尺寸。

## [2026-03-10] Revision 15.16 - 按鈕順序與對齊修正
- **順序保全**：交換建議詞管理器操作按鈕順序（✏️ 在前，🗑️ 在後）。
- **對齊優化**：優化「操作」欄位的水平與垂直置中對齊，提升視覺對稱性。

## [2026-03-10] Revision 15.15 - 表格佈局與幾何優化
- **佈局解鎖**：移除 1200px 寬度限制，恢復自由同步。
- **最小規格**：加入 800x600 最小視窗限制，確保基本操作空間。
- **內容對齊**：實作表格內容水平置中，優化操作欄位垂直對齊。
- **彈性收縮**：修正空表格下的視窗收縮彈性。

## [2026-03-10] Revision 15.14 - 表格幾何穩定性強化
- **換行強制**：將 Viewport 同步寬度上限調降至 1200px (後於 15.15 移除限制)，並在辭典表格中強制執行換行。
- **防止溢出**：使用 `set_max_width` 與 `add(Label)` 強制執行換行，防止表格無限向右延伸引發水平捲動。

## [2026-03-10] Revision 15.9 - Viewport 終極三重防禦方案 (V6)
- **三重防禦 (Triple Shield)**：
    1. **點擊預延遲**：點擊「📖」按鈕後實施 0.5 秒 (30幀) 靜默期。
    2. **延後亮顯**：視窗創立後保持 `visible(false)` 直到穩定運行 60 幀。
    3. **視覺修復**：分頁按鈕改用 `add_sized` 固定寬度，解決文字「切邊」問題。
- **背景消色**：直接注入 `visuals.window_fill` 以確保顯現瞬间無白閃。

## [2026-03-10] Revision 15.5 - Viewport 幾何穩定性終極修復 (V4)
- **隱形啟動策略**：Viewport 初始設置為 `visible(false)`，完成渲染後才顯現，消除白屏閃現。
- **UI 視覺優化**：增加建議詞分頁按鈕 Frame 的 `inner_margin`。

## [2026-03-10] Revision 15.4 - Viewport 幾何穩定性修復 (V3)
- 強化視窗幾何引導與座標鎖定。

## [2026-03-10] Revision 15.3 - 視窗幾何穩定性與啟動平滑化 (V2)
- **同步鎖定期**：實作 30 幀鎖定期，防止 OS 初期錯誤座標干擾。
- **主題預渲染**：Viewport 閉包開頭立即初始化視覺風格。

## [2026-03-10] Revision 15.2 - UI 視窗穩定性修復
- **座標修復**: 確保子視窗在開啟的首幀即套用儲存的幾何設定。
- **狀態追蹤**: 實作從子視窗同步 `outer_rect` 回主程式，使調整後的狀態能即時持久化。

## [2026-03-10] Revision 15.1 - Git 版本控制與文檔同步
- **Git 初始化**: 啟動了 Git 倉庫，建立 `.gitignore` 並完成首次提交。
- **文檔索引**: 更新 `AI_CONTEXT.md` 並新增 `GIT_GITHUB_GUIDE.md`。
- **依賴優化**: 移除未使用的依賴項（async-recursion, futures, rand, tempfile）。

## [2026-03-10] Revision 14.6 - 遷移邏輯與 UI 文字優化
- **自動遷移**: 實作「修改即移入」邏輯，官方條目編輯後自動轉存使用者字典。
- **標籤精製**: 取代對話框標籤更新為「原Value:」與「新Value:」。

## [2026-03-10] Revision 14.5 - 核心邏輯修補與 UI 精製
- **取代修復**: 解決雙分頁取代後的 UI 即時反應問題。
- **按鈕優化**: 「存檔」與「取消」順序對調，確保主要按鈕置右。

## [2026-03-10] Revision 14.4 - 功能修正與鍵盤化優化
- **鍵盤快捷**: 為新增、取代及編輯模式新增 Enter 鍵支援。
- **對話框解鎖**: 移除對話框強制中央錨點，支援自由移動。

## [2026-03-10] Revision 14.3 - 表格佈局精製與按鈕對齊優化
- **欄位固定**: 將 Key 欄位改為固定寬度 Label，解決水平擠壓問題。
- **操作置右**: 表格操作按鈕改為靠右對齊排列。

## [2026-03-10] Revision 14.2 - 佈局穩定性與同步強化
- **捲動鎖定**: 禁用 `ScrollArea` 水平捲動，防止表格遞迴增長。
- **無縫同步**: 實作主視窗主題變動時子視窗的即時重繪。

## [2026-03-10] Revision 14.1 - 建議詞管理器功能修復與 UI 強化
- **CRUD 校正**: 修復編輯、刪除邏輯及 JSON 檔案雙開功能。
- **UI 強化**: 標題置中與空白狀態引導提升使用者體驗。

## [2026-03-10] Revision 13 - 進度選取同步與視覺純淨化
- **選取即同步**: 修正檔案選取後的計數器顯示。
- **視覺純淨**: 移除冗餘點點點及不必要的標頭區域鎖定。

## [2026-03-10] Revision 12.3 - 掃描進度即時反饋與標籤解鎖
- **即時掃描**: 修正大資料夾掃描時的進度條反應。
- **標籤視覺解鎖**: 翻譯期間 Label 不變灰，精確鎖定交互組件。

## [2026-03-10] Revision 12.2 - 按檔案彙整翻譯與即時進度
- **全檔案處理**: 重構翻譯階段，提高進度回報頻率。

## [2026-03-10] UI 極致還原與系統精簡 (Revision 10 & 11)
- **CKIP 徹底移除**: 移除 Python 分詞相依性，回歸原生 LLM 理解。
- **UI 還原**: 恢復 Grid 對齊、條紋表格與 NavajoWhite 專業配置。

## [2026-03-09] 移除 CKIP 系統與建議詞管理器重構
- **架構重塑**: 建議詞管理器雙頁化與辭典規範化。

## [2026-03-08] 辭典持久化分流與原子提取演算法重構
- **演算法升級**: 實作歧義島嶼提取與原子化建議產出。

## [2026-03-07] UI 佈局修復與量子重構 (Quantum Refactor)
- **子視窗同步**: 實作 Viewport 樣式繼承與通訊穩定化。

## [2026-03-06] 深度還原 0304 基準邏輯 (2-Phase 兼容版)
- **邏輯對位**: 整合 2-Phase 架構與 0304 核心匹配演算法。

## [2026-03-05/06] 全方位架構改進與效能重構
- **效能重構**: 實作平行掃描與原生下載器。

## [2026-03-05] 術語系統優先級與 Unicode 安全規範
- **安全性**: unicode 轉義與 API 設定面板恢復功能。

## [2026-03-04] Ollama/Gemini 整合與 Bug 修復
- **核心修復**: UTF-8 邊界修復與引號處理優化。

## [2026-03-02 及以前] 基礎架構與除錯
- **基礎建立**: 非同步驅動、DPAPI 加密與日誌效能優化。
