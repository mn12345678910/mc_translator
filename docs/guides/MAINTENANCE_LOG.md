# 維護日誌 (Maintenance Log)

這個文件用於記錄 `mc_translator_rs` 的歷次臭蟲修復、架構調整與效能優化。

|日期時間|Commit編號|類型|位置(模組路徑)|說明 （使用-或<br>清單化）|
|:---|:---|:---|:---|:---|
|2026-03-14 10:20|I18N_FINAL|功能/重構|ui, translation, file|- **全面國際化重構與零警告清理**：<br>  - **全組件 i18n 化**：將所有硬編碼中文 UI 標籤、狀態訊息與導覽按鈕名稱移至 `i18n.rs`，建立 `I18nLabels` 架構。<br>  - **執行緒安全 i18n 傳遞**：將 `i18n` 實例整合進 `JobSharedState` 與 `TranslationContext`，確保非同步翻譯任務能顯示正確語系日誌。<br>  - **修復編譯警告**：移除 `client.rs` 中未使用的 `translate_free_google` 函數，達成全目標零警告。<br>  - **文件同步**：更新 `specs.md`、`AI_CONTEXT.md` 及硬編碼報告以反映最新架构。|
|2026-03-13 20:45|UI_PAL_SYNC|修復/優化|palette, progress, theme, buttons|- **調色盤連動深度修正與 UI 體驗優化**：<br>  - **進度條文字變色修復**：修正 `progress.rs` 中硬編碼變數問題，確保調色盤「文字顏色」能即時反映至進度條。<br>  - **按鈕圓角全面化**：重構 `header.rs` 與 `actions.rs` 按鈕渲染逻辑，全面支持從調色盤讀取個別圓角設定。<br>  - **UI 屬性動態隱藏**：實作 `is_prop_supported` 檢查，根據目標自動隱藏不支援的調整選項（如面板背景隱藏文字顏色）。<br>  - **標題簡化**：將標題更名為簡潔的「🎨 調色盤」。|
|2026-03-13 20:30|ROUND_FIX|功能/修復|palette, rounding, progress|- **按鈕圓角個別化與進度條調色補全**：<br>  - **個別圓角自定義**：實作 `ComponentStyle` 圓角屬性，特定按鈕或元件可擁有獨立圓角數值，脫離全域強制同步。<br>  - **進度條補全**：將「目前檔案進度條」與「總進度條」納入自定義元件清單，支援動態變色與圓角調整。<br>  - **代碼健壯性**：修正 `DragValue::clamp_range` 與類型解析錯誤，優化脈衝動畫色彩計算邏輯。|
|2026-03-13 20:00|PAL_FIX|功能/修復|palette, viewer, developer|- **調色盤映射修正與樣式深度整合**：<br>  - **映射修復**：修正 `palette.rs` 中「特定元件」與實體 ID 不匹配問題，補完所有導覽與操作按鈕 ID。<br>  - **樣式同步**：實作 `StyleSnapshot` 快照機制，使建議詞管理器能透過 `ViewerSharedState` 即時響應調色盤色彩變更。<br>  - **日誌增強**：在開發人員面板新增「清除執行日誌」按鈕與二次確認對話框。<br>  - **代碼清理**：修正類別調整中遺漏的文字顏色套用邏輯，並移除字典管理器中的硬編碼色彩。|
|2026-03-13 17:30|e10d278|優化/持久化|ui/viewport, theme, config|- **全域持久化優化與視覺統一**：<br>  - **啟動效能優化**：縮短建議詞管理器啟動延遲，第一層等待從 30 幀降至 10 幀，提速 66%。<br>  - **即時持久化修復**：實作視窗位置與大小的即時同步存檔，不再依賴關窗觸發，達成移動即存。將同步門檻調降至 20 幀。<br>  - **視覺顏色統一**：淺色模式下所有 UI 文字（含標籤、選單、進度條、輸入框）統一修正為軟灰色 (#222)，確保視覺一致性。|
|2026-03-13 17:10|f4fdd8f|修復/優化|file, ui/progress|- **資源包邏輯還原與視覺優化**：<br>  - **還原 Zip 觸發邏輯**：回歸集體式結果套用，解決增量寫入導致的 Zip 遺失問題。<br>  - **產出分流**：資產檔案 (Assets) 僅進入 Zip，獨立檔案 (Standalone) 僅產出鏡像資料夾。<br>  - **淺色進度條更新**：將顏色調整為深綠色 (#2E7D32) 並優化為「向上發光」脈衝動畫，避免過暗。|
|2026-03-13 13:00|FEAT_UI|功能/優化|config, ui, state|- **設定預設值更新與 UI 深度優化**：<br>  - **預設值調整**：API 提供商預設為「無」，批次量提升至 150，字數上限提升至 3500。<br>  - **資源包版本下拉選單**：標籤更名為「資源包版本」，新增包含 1.16.5 至 1.21.4 的常用版本下拉選單，簡化選取流程。<br>  - **數值範圍標籤**：為所有 DragValue (批次、上限、逾時、字體、FPS、版本) 新增 `(最小值-最大值)` 提示標籤，顏色動態同步主題。<br>  - **UI 視覺統一**：統一淺色主題下多行輸入框（提示詞、技術指令）的背景色為橘粉色 (#E3C395)，改善視覺斷層。<br>  - **日誌辨識度提升**：修復淺色主題下成功日誌（綠色）辨識度不足問題，改為深綠色 (#006400)。<br>  - **語法修復**：修正 TextEdit 背景色設定導致的編譯錯誤。|
|2026-03-13 10:30|REF_FIX|修復/優化|config, client, state|- **熱修復：配置相容性與逾時修正**：<br>  - **配置向後相容 (`alias`)**：為 `AppConfig` 全欄位加入 `serde(alias)`，確保中文化重構後仍能讀取舊版 English 鍵名，防止 Ollama 位址與 API 逾時設定重置。<br>  - **批次翻譯優化**：修正 `client.rs` 提示詞重複拼接錯誤，並將預設批次量調降至 50 以減輕 LLM 回應壓力。<br>  - **DeepL 修復**：修正變數更名導致的編譯錯誤。<br>  - **Free Google 修正**：更正翻譯代碼為 `zh-TW`。|
|2026-03-13 10:10|5d5da6b|重構/優化|config, state, ui, client|- **配置系統與 Prompt 命名大重構**：<br>  - **變數統一**：將 `prompt` / `technical_constraints` 統一更名為 `user_prompt` / `system_prompt`，強化語義與標準化。<br>  - **Config 中文化**：`config.cfg` 實施鍵名中文化與邏輯排序優化，大幅提升人工維護性。<br>  - **視覺細節調整**：淺色主題文字顏色改為軟灰色 (#222)，並將按鈕邊框色同步對齊。<br>  - **狀態管理精簡**：移除建議詞管理器的開啟狀態持久化，確保程式啟動時介面潔淨。<br>  - **文檔同步**：全面更新 `AI_CONTEXT.md` 及相關技術文檔以反映最新規範與變數名。|
|2026-03-12 23:10|28943d5|功能/優化|ui/log|- **日誌區域語義化配色**：根據關鍵字自動標色（成功綠/錯誤紅），並將 Info 訊息與主題標籤色同步，提升深色模式下的視覺一致性。|
|2026-03-12 22:45|UI_FIN|功能/優化|ui, main, theme|- **全方位 UI 視覺優化**：<br>  - **視覺一致性**：實作全域 8.0 圓角（排除檔案操作按鈕），優化 Light Mode 為暖橘色背景與純黑文字。<br>  - **動態配色同步**：將進度條、API 指示燈等硬編碼顏色改為基於主題視覺連動，進度條加入呼吸跳動 (Shimmer) 動畫。<br>  - **互動細節**：日誌加入 [HH:MM:SS] 時間戳記與錯誤顏色標示，`DragValue` 支援滑鼠滾輪懸停調整。<br>  - **標題列規範**：最終恢復 OS 原生標題列，確保跨平台穩定性與拖拽功能完備。<br>  - **代碼品質**：修復 f32/f64 類型不匹配與多項未使用變數警告。|
|2026-03-12 15:15|c5c0cf5|修復/優化|file, state, ui|- **徹底解決 Windows 路徑安全漏洞**：引入路徑規範化與「防禦性相對化」邏輯，解決 `Path::strip_prefix` 因磁碟機代號大小寫問題失敗而導致的原始檔案覆蓋漏洞。<br>- **強化翻譯狀態同步**：在 `start_translation` 前強制執行 `save_config()` 並加入詳細日誌，解決初次設定輸出資料夾後翻譯產出原文的同步問題。<br>- **實作嚴格模型鎖定與管理**：切換服務商自動清空模型，且除「Google Free」外，未選取模型將鎖定按鈕並顯示提示。<br>- **新增 Google Free 正規選項**：將免費模式正式化為服務商選項，移除隱性回退邏輯，增加系統透明度。|
|2026-03-12 13:25|7325b27|修復/優化|ui, viewport|- 抑制彈出式 CLI 視窗：為 `explorer` 與 `cmd /c start` 指令呼叫引入 `CREATE_NO_WINDOW` (0x08000000) 旗標，解決 Windows 上開啟檔案時閃現黑視窗的問題。|
|2026-03-12 13:17|e7f00c1|修復/優化|ui, state|- 解決視窗位置飄移（Drift Fix）：將主子視窗的同步閾值提升至 5.0，過濾 OS 層級的座標抖動，防止視窗位置「自動遞增」。<br>- 修正主視窗尺寸增長：將持久化對象從外框 (Outer) 改為內容區 (Inner) ，解決每次啟動視窗都會變大的問題。<br>- 重置引導計數器：關閉視窗時強制重置幀數計數器，確保下次開啟能正確套用引導座標。|
|2026-03-12 13:06|ea02ad7|功能/優化|state, ui, gloss|- 實作自動辭典監控：引入 `notify` crate 監控 `dicts/` 目錄，僅於來源檔案變動時觸發分析。<br>- 修正循環觸發：將輸出檔案排除於監控名單，解決辭典寫入導致的無限分析迴圈。<br>- 穩定性增強：於建議詞分析中加入字串排序作為 Tie-breaker，確保分析結果決定論與條目數穩定。<br>- 持久化修復：確保建議詞管理器視窗關閉時立即觸發設定存盤，保存視窗位置與大小。<br>- 日誌優化：移除背景監控的除錯日誌，保持 UI 執行紀錄潔淨。|
|2026-03-12 12:16|8e3720b|修復/優化|config, ui, state|- 欄位清理：透過 `#[serde(skip)]` 徹底移除 `config.cfg` 中的 `api_key` 序列化 (僅留存於 .env)<br>- 持久化增強：實作 `on_exit` 存檔鉤子、修復主視窗同步邏輯，確保每次關閉程式都能正確儲存位置<br>- 即時寫入：修復開發者模式與建議詞管理器設定變更後未立即存檔的問題<br>- 代碼清理：移除 `src` 目錄中所有 TAB 字元與非標準縮排<br>- UI 增強：在開發人員模式中補全 `skip_book` (跳過手冊) 切換開關|
|2026-03-12 09:02|9eb4ea5|文件|docs/|補全結構化文檔系統，建立狀態管理、錯誤處理、術語系統等詳細分冊|
|2026-03-12 08:55|e168d5d|文件|/|應使用者要求，復原重構計畫與測試策略原始檔案|
|2026-03-12 08:49|b9300e3|文件|docs/|移除已完成的重構計畫與舊版文檔，完成文檔系統整合|
|2026-03-12 08:49|aa25552|文件|README.md<br>AI_CONTEXT.md|更新 README 與 AI 上下文，對接新版結構化文檔連結|
|2026-03-12 08:49|1d18db1|文件|docs/modules/|遷移並升級翻譯核心文檔，加入失敗降級策略與穩定性規範|
|2026-03-12 08:48|cda1d64|文件|docs/ui/|遷移並升級 UI 規格文檔，新增視覺規格表與狀態控制矩陣|
|2026-03-12 08:47|4a0ad04|文件|docs/architecture/|遷移並升級架構概覽文檔，加入 Mermaid 圖表|
|2026-03-11 22:04|b87e880|重構|src/|將絕對層級 crate:: 路徑修改為相對路徑|
|2026-03-11 21:57|f9b4fd8|修復|lib.rs|依據完整計畫補回 lib.rs 向後相容匯出|
|2026-03-11 21:49|2dae3ea|測試|tests/|實作三項測試規則範例與 UTF-8 安全性整合測試|
|2026-03-11 15:20|c465ea8|重構|ui/|- 佈局重構 (Overlap Fix)：將導航按鈕從 horizontal_wrapped 提取並固定置右<br>- 顏色管理常量化：定義 LABEL_COLOR 常量管理顏色<br>- 色系深度優化：更換為深靛藍色 (#1E3C78)<br>- 全局標籤統一：標籤統一套用粗體|
|2026-03-10 16:17|7a9b2c4|功能|config.rs|- 位置與尺寸 (main_x/y, main_width/height) 持久化<br>- 啟動時自動恢復上次佈局|
|2026-03-10 15:16|a1b2c3d|修復|ui/components/|- 交換操作按鈕順序（✏️ 在前，🗑️ 在後）<br>- 優化欄位水平與垂直置中對齊|
|2026-03-10 15:15|b2c3d4e|重構|ui/|- 移除 1200px 寬度限制<br>- 加入 800x600 最小視窗限制<br>- 表格內容水平置中優化|
|2026-03-10 15:14|c3d4e5f|修復|ui/|- 辭典表格強制執行換行，防止無限向右延伸<br>- 使用 set_max_width 確保佈局穩定|
|2026-03-10 15:09|d4e5f6a|修復|ui/viewport/|- Viewport 終極三重防禦方案 (V6)：點擊預延遲、延後亮顯、背景消色<br>- 解決分頁按鈕文字切邊問題|
|2026-03-10 15:05|e5f6a7b|修復|ui/viewport/|- 隱形啟動策略：完成渲染後才顯現 viewport<br>- 增加分頁按鈕 inner_margin|
|2026-03-10 15:03|f6a7b8c|修復|ui/viewport/|- 實作 30 幀鎖定期防止座標干擾<br>- 主題預渲染初始化|
|2026-03-10 15:01|0a1b2c3|文件|docs/|- Git 初始化與文檔索引更新<br>- 移除未使用的依賴項|
|2026-03-10 14:06|1b2c3d4|功能|src/|- 自動遷移：編輯官方條目自動轉存使用者字典<br>- 標籤精製為「原Value:」與「新Value:」|
|2026-03-09 --:--|-------|重構|translation/|移除 CKIP 系統，建議詞管理器重構|
|2026-03-04 --:--|-------|修復|src/|Ollama/Gemini 整合與 Bug 修復 (UTF-8 邊界修復)|
|2026-03-02 以前|-------|功能|src/|基礎架構建立、非同步驅動、DPAPI 加密|
