# Git 與 GitHub 使用指南

## 基本流程

1. 修改檔案
2. `git add .`
3. `git commit -m "<繁體中文訊息>"`
4. `git push`

## 專案慣例

- 分支名稱請使用 `codex/` 前綴
- Commit 訊息請使用繁體中文
- `docs/guides/MAINTENANCE_LOG.md` 新增紀錄須置頂

## 常用指令

- 查看狀態：`git status`
- 查看歷史：`git log --oneline`
- 還原未提交變更：`git restore .`
